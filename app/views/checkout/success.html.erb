<!-- app/views/checkout/success.html.erb -->
<%= render "shared/navbar" %>
<div class="container py-5 text-center">
  <h1 class="mb-3">Payment successful 🎉</h1>
  <% if @session_id.present? %>
    <p class="text-muted">Session ID: <code><%= @session_id %></code></p>
  <% end %>
  <%= link_to "Back to dashboard", root_path, class: "btn btn-snoofly" %>
</div>
<%= render "shared/footer" %>

<!-- app/views/checkout/cancel.html.erb -->
<%= render "shared/navbar" %>
<div class="container py-5 text-center">
  <h1 class="mb-3">Checkout cancelled</h1>
  <p class="text-muted">No charge was made.</p>
  <%= link_to "Return to pricing", pricing_path, class: "btn btn-snoofly-outline" %>
</div>
<%= render "shared/footer" %>